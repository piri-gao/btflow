BTFlow Overview

BTFlow is a lightweight agent framework built on behavior trees.
It emphasizes modular nodes, composable workflows, and tool integration.

Memory in BTFlow uses a minimal triad: Record, Store, Retriever.
You can ingest documents into Memory and retrieve relevant chunks for RAG.
